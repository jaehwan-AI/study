Xgboost에서의 결측치

-> 결측치를 인식하고 자동적으로 처리한다(채워넣는다) = Sparsity-aware(희소성 인식 알고리즘)

-----------------------------------------------------------

interpolate(보간법) - 결측치를 처리하는 방법

-> 선형으로 비례하는 방식
-> 몇 개의 포인트들로 추정하여 인접한 점들 사이를 다항식 함수로 계산/추정하는 방법
-> 값에 연속성이 있을 경우 매우 유용하다